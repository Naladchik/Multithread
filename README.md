nCurses shall be installed.

To compile use:
  cc huff.c main.c -o main -lncurses

directories 
  /to_compress
  /compressed 
should should be added to the project directory

Limitation:
Anly ASCII coded files can be zipped

